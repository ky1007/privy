// import { connect } from 'react-redux';
// import ReflectionIndex from './reflection_index';
// import { selectAllReflections } from '../../reducers/selectors';
// import { fetchReflections, destroyReflection } from '../../actions/reflection_actions';

// const mapStateToProps = ({ reflections }) => ({
//   reflections: selectAllReflections(reflections),
// });

// const mapDispatchToProps = dispatch => ({
//   fetchReflections: entryId => dispatch(fetchReflections(entryId)),
//   destroyReflection: entryId => dispatch(destroyReflection(entryId)),
// });

// export default connect(mapStateToProps, mapDispatchToProps)(ReflectionIndex);

// DELETE THIS FILE once you've verfied that entry show works with reflections showing