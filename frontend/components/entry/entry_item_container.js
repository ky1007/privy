import { connect } from 'react-redux';
import { X } from '../../actions/X';
import X from './X';


const mapStateToProps = ({  }) => ({
});

const mapDispatchToProps =  => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(X);
